# Hono + React + TypeScript + Vite + Bun

This is a fullstack template that uses Hono + React (Vitejs) as front and backend frameworks.

This template uses this packages:

    - Router: Tanstack Router
    - Forms: Tanstack Form
    - Server Calls: Tanstack Query
